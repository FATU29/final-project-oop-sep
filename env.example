# MySQL Configuration
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=sep_demo
MYSQL_USER=sep_user
MYSQL_PASSWORD=sep_password
MYSQL_PORT=3306

# PostgreSQL Configuration
POSTGRES_DB=sep_demo
POSTGRES_USER=sep_user
POSTGRES_PASSWORD=sep_password
POSTGRES_PORT=5432

# Application Database Selection
# Options: mysql, postgresql
DB_TYPE=mysql

# Connection Strings (auto-generated, but can be overridden)
# MySQL: jdbc:mysql://localhost:${MYSQL_PORT}/${MYSQL_DATABASE}?useSSL=false&serverTimezone=UTC
# PostgreSQL: jdbc:postgresql://localhost:${POSTGRES_PORT}/${POSTGRES_DB}

